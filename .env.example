# ============================================
# 博客系统统一环境配置示例
# 部署步骤：cp .env.example .env 然后编辑实际值
# ============================================

# 项目信息
PROJECT_NAME=linglong-blog
# 环境配置
# production | development
ENVIRONMENT=production

# 服务端口
NGINX_PORT=8080
BACKEND_PORT=8000

# 安全配置
# 使用以下命令生成强密钥：
# JWT 密钥（生成命令：python -c "import secrets; print(secrets.token_urlsafe(32))"）
SECRET_KEY=your_secret_key_here

# 管理员账户
# 生成一个安全密码（建议12位，包含大小写字母、数字和符号）
# python -c "import secrets, string; chars = string.ascii_letters + string.digits + string.punctuation; print(''.join(secrets.choice(chars) for i in range(12)))"
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_strong_password_here

# Astro 路径配置
ASTRO_PROJECT_PATH=/code/lingLong
ASTRO_CONTENT_PATH=/code/lingLong/src/contents/posts
ASTRO_DIST_PATH=/code/lingLong/dist

# CORS 配置
# 留空使用 Nginx 反向代理模式
# 开发环境：留空使用默认本地端口 (localhost:4321-5000)
# 生产环境（Nginx反向代理）：留空
# 生产环境（直接暴露）：填写域名，如 https://yourdomain.com
# 示例：
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# ALLOWED_ORIGINS=https://blog.example.com:443,https://admin.example.com:443
ALLOWED_ORIGINS=
